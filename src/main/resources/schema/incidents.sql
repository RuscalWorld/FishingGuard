CREATE TABLE IF NOT EXISTS `incidents` (`id` INTEGER PRIMARY KEY AUTOINCREMENT, `member_id` VARCHAR(50) NOT NULL, `guild_id` VARCHAR(50) NOT NULL, `text` TEXT, `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)